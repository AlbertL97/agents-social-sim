# Social-sim dependencies. Versions pinned from the verified working venv.
# Install into the project venv:
#   uv venv .venv --python 3.12 && uv pip install -r requirements.txt
# (or: python -m venv .venv && .venv/bin/pip install -r requirements.txt)

# Concordia multi-agent simulation framework (the engine).
gdm-concordia==2.4.0

# Google Gemini — the ONLY LLM/embedding provider used by the simulation.
# (Per the project LLM-budget rules: do NOT reuse opencode subscriptions.)
google-genai==2.16.0

# Supabase Free Postgres client (persistence backend).
supabase==2.31.0

# Test runner — the whole suite runs offline in --dry-run with no key.
pytest==9.1.1
